import Layout from '../components/Layout';

export default function Start() {
  return (
    <Layout>
      <main style={{maxWidth:1100, margin:'0 auto', padding:'2rem'}}>
        <h1>Start</h1>
        <p>Build forms and CTAs similar to Better start page — signup / pre-qualification flows (simple mocked forms are fine).</p>
      </main>
    </Layout>
  );
}
